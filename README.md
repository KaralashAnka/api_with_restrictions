# 🚀 Быстрый старт



### Ручная установка (Windows или если скрипт не работает)

```bash
# Установить зависимости
pip install -r requirements.txt

# Создать .env
copy .env.example .env  # Windows
cp .env.example .env    # Linux/Mac

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser
```

### Настройка базы данных

Создайте базу данных в PostgreSQL:

```sql
CREATE DATABASE advertisements_db;
```


### Создание токена для пользователя

```bash
# Запустите сервер
python manage.py runserver

# Откройте админку: http://localhost:8000/admin/
# Войдите с учётными данными суперпользователя
# Перейдите в "Tokens" и создайте токен для пользователя
```

### Тестирование API

Используйте файл `requests-examples.http` с расширением REST Client в VS Code или:

```bash
# Просмотр объявлений (без авторизации)
curl http://localhost:8000/api/advertisements/

# Создание объявления (с авторизацией)
curl -X POST http://localhost:8000/api/advertisements/ \
  -H "Authorization: Token ВАШ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{"title": "Test", "description": "Test", "status": "OPEN"}'
```

### 7. Запуск тестов

```bash
python manage.py test
```

## 📁 Структура проекта

```
advertisements_project/
├── manage.py                   # Django management
├── requirements.txt            # Зависимости
├── README.md                   # Полная документация
├── QUICKSTART.md              # Эта инструкция
├── CODESTYLE.md               # Стандарты кодирования
├── ADDITIONAL_FEATURES.md     # Дополнительные задания
├── requests-examples.http     # Примеры HTTP запросов
├── setup.sh                   # Скрипт автоустановки
├── advertisements/            # Приложение
│   ├── models.py             # Модели
│   ├── serializers.py        # Сериализаторы
│   ├── views.py              # Views
│   ├── permissions.py        # Права доступа
│   ├── filters.py            # Фильтры
│   ├── urls.py               # URL маршруты
│   ├── admin.py              # Админка
│   └── tests.py              # Тесты
└── advertisements_project/    # Настройки
    ├── settings.py
    └── urls.py
```

## ✅ Реализованные возможности

- ✅ CRUD операции для объявлений
- ✅ Аутентификация по токену
- ✅ Права доступа (только создатель может изменять)
- ✅ Валидация (не более 10 открытых объявлений)
- ✅ Фильтрация по дате и статусу
- ✅ Rate limiting (10/мин для неавториз., 20/мин для авториз.)
- ✅ Pagination
- ✅ Django админка
- ✅ Тесты
- ✅ Документация

